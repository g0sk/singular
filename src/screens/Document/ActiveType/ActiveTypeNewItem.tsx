import React from 'react';
import {NewActiveTypeScreenProps} from 'types';

export const ActiveTypeNewItem: React.FC<NewActiveTypeScreenProps> = (
  {
    //route,
    //navigation,
  },
) => {
  return null;
};
