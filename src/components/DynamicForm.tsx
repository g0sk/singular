export const A = 0;
/* export const a = 2;
import React, {useEffect, useState} from 'react';
import {View, Text, SimpleTextInput as TextInput, Dropdown} from 'components';
import {TouchableOpacity, StyleSheet} from 'react-native';
import {translate} from 'core';
import {DynamicFormProps} from 'types';

export function DynamicForm<T>({}: //basicAttributes,
//customAttributes,
DynamicFormProps<T>) {
  const [attributes, setAttributes] = useState<T[]>();
  const [customAttributes, setCustomAttributes] = useState<T[]>([]);

  useEffect(() => {
    setAttributes([...basicAttributes]);
    setCustomAttributes([...customAttributes]);
  }, [basicAttributes, customAttributes]);

  return (
    <View>
      {/* <View style={styles.basicContainer}>
        <DynamicFields items={basicAttributes} />
      </View>
      <View style={styles.customContainer}>
        <DynamicFields items={customAttributes} />
      </View> }
    </View>
  );
}

const styles = StyleSheet.create({
  basicContainer: {},
  basicAttributes: {},
  customAttributes: {},
  customContainer: {},
  type: {},
});
 */
